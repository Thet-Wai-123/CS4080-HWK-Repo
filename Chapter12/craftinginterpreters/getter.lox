class Circle {
    init(radius) {
        this.radius = radius;
    }
    
    area () {
        return 3.14159 * this.radius * this.radius;
    }
}

var circle = Circle(5);
print "Area: " + circle.area();